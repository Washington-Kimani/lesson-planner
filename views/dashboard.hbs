<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <!--    general styles-->
    <link rel="stylesheet" href="/styles/style.css">
    {{!-- tailwind --}}
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .dashboard {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .dashboard .box {
            background-color: #007bff;
            color: white;
            padding: 20px;
            margin: 10px;
            text-align: center;
            border-radius: 8px;
            cursor: pointer;
            flex: 1 1 40%;
            text-decoration: none;
        }

        .dashboard .box:hover {
            background-color: #0056b3;
        }

        .dashboard .line {
            flex-basis: 100%;
            height: 1px;
            background-color: #007bff;
            margin: 10px 0;
        }

        .logout {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            text-align: center;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: block;
            width: fit-content;
            margin: 20px auto 0;
        }

        .logout:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <div
        class="fixed w-full top-0 left-0 flex justify-center items-center bg-gradient-to-b from-emerald-500 via-emerald-300 to-teal-900 py-4 px-6">
        <nav class="flex space-x-4">
            <a href="/" class="text-white hover:text-pink-400">Dashboard</a>
            <a href="/teachers" class="text-white hover:text-pink-400">Teachers</a>
            <a href="/students" class="text-white hover:text-pink-400">Students</a>
            <a href="/lesson_plans" class="text-white hover:text-pink-400">Lesson Plans</a>
        </nav>
    </div>
    <h1>User Dashboard</h1>
    <div class="container flex flex-col mt-20">
        <div class="w-full flex gap-3 mb-3 justify-center">
            <p>Welcome</p>
            <p>{{user.fullnames}}</p>
        </div>
        <div class="w-full flex gap-3 mb-3 justify-center">
            <p>Reg No:</p>
            <p>{{user.registration_number}}</p>
        </div>
        <div class="dashboard">
            <a href="/profile" class="box active" id="teacher-detail">Teacher Details</a>
            <a href="/students" class="box active" id="student-detail">Student Details</a>
            <div class="line"></div>

            <div class="w-full flex flex-col justify-around items-start">
                <h2>My Current Subjects:</h2>
                <section class="w-full flex">
                    <ul class="list-disc">
                        <li class="pl-4 w-full flex items-start">{{user.subject_one}}</li>
                        <li class="pl-4 w-full flex items-start">{{user.subject_two}}</li>
                    </ul>
                </section>
            </div>
        </div>
        <form method="POST" action="/logout" class="w-full text-center">
            <button type="submit" class="px-3 py-1 bg-blue-400 rounded shadow-lg shadow-black-500 hover:bg-blue-700">Log
                Out</button>
        </form>
    </div>
</body>

</html>