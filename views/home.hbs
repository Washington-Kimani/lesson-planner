<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <!--    general styles-->
    <link rel="stylesheet" href="/styles/style.css">
    {{!-- tailwind --}}
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .nav-links {
            text-align: center;
            margin: 20px 0;
        }

        .nav-links a {
            text-decoration: none;
            color: #007bff;
            margin: 0 10px;
            font-size: 18px;
        }

        .nav-links a:hover {
            text-decoration: underline;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .dashboard {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .dashboard .box {
            background-color: #007bff;
            color: white;
            padding: 20px;
            margin: 10px;
            text-align: center;
            border-radius: 8px;
            cursor: pointer;
            flex: 1 1 40%;
            text-decoration: none;
        }

        .dashboard .box:hover {
            background-color: #0056b3;
        }

        .dashboard .line {
            flex-basis: 100%;
            height: 1px;
            background-color: #007bff;
            margin: 10px 0;
        }

        .logout {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            text-align: center;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: block;
            width: fit-content;
            margin: 20px auto 0;
        }

        .logout:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <h1>User Dashboard</h1>
    <div class="nav-links">
        <nav>
            <a href="/">Home</a>
            <a href="/dashboard">Dashboard</a>
            <a href="/teachers">Teachers</a>
            <a href="/students">Students</a>
            <a href="/subjects">Subjects</a>
        </nav>
    </div>
    <div class="container flex flex-col">
        <div class="w-full flex gap-3 mb-3 justify-center">
            <p>Welcome</p>
            <p>{{user.fullnames}}</p>
        </div>
        <div class="w-full flex gap-3 mb-3 justify-center">
            <p>Reg No:</p>
            <p>{{user.registration_number}}</p>
        </div>
        <div class="dashboard">
            <a href="" class="box active" id="teacher-detail">Teacher
                detail</a>
            <a href="" class="box active" id="student-detail">Student detail</a>
            <div class="line"></div>
            <a href="" class="box active" id="subject-1">Subject 1</a>
            <a href="" class="box active" id="subject-2">Subject 2</a>
        </div>
        <form method="POST" action="/logout" class="w-full text-center">
            <button type="submit" class="px-3 py-1 bg-blue-400 rounded shadow-lg shadow-black-500 hover:bg-blue-700">Log Out</button>
        </form>
    </div>
</body>

</html>